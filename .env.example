# Sao chép file này thành .env (KHÔNG commit .env — đã có trong .gitignore). Bài 20 dạy phần này.
BASE_URL=https://www.saucedemo.com
SAUCE_USER=standard_user
SAUCE_PASS=secret_sauce

# Dùng cho bài API ↔ UI (Bài 19) nếu cần:
# API_BASE_URL=
